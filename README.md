# Адаптивная очередь

## Добавление в локальный репозиторий Maven

Для того, чтобы использовать адаптивную очередь в проекте, необходимо добавить её в локальный репозиторий Maven. Для этого выполните следующую команду в терминале:

```./gradlew publishToMavenLocal```